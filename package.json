{
  "name": "figma-hierarchy-export-plugin",
  "version": "1.0.0",
  "description": "Export selected nodes and their children by hierarchy levels",
  "main": "code.js",
  "scripts": {
    "build": "esbuild code.ts --bundle --outfile=code.js --target=es6",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.82.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}